[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/webhook-server/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
# SQLite + multi-instance = corruption. Do not raise this.
numReplicas = 1
